# Blog Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter le module Blog front-end : liste et détail des articles publiés, formulaire de commentaire pour les utilisateurs connectés.

**Architecture:** Pattern miroir du module Pages existant. Un service `CommentaireHandler` isole la logique de soumission. Le contrôleur `ArticleController` gère les routes GET (index/show) et POST (commenter). Les commentaires soumis passent en statut `en_attente` jusqu'à modération dans EasyAdmin.

**Tech Stack:** Symfony 7, Doctrine ORM, Twig + Bootstrap 5, AsciiSlugger, PHPUnit (WebTestCase + TestCase)

---

## Fichiers concernés

| Action | Fichier |
|---|---|
| Modifier | `src/Entity/Article.php` |
| Modifier | `src/Repository/ArticleRepository.php` |
| Modifier | `templates/base.html.twig` |
| Créer | `src/Service/CommentaireHandler.php` |
| Créer | `src/Form/CommentaireType.php` |
| Créer | `src/Controller/ArticleController.php` |
| Créer | `templates/blog/index.html.twig` |
| Créer | `templates/blog/show.html.twig` |
| Créer | `tests/Entity/ArticleSlugTest.php` |
| Créer | `tests/Service/CommentaireHandlerTest.php` |
| Créer | `tests/Controller/ArticleControllerTest.php` |

---

## Task 1 : Slug sur l'entité Article

**Files:**
- Modify: `src/Entity/Article.php`
- Create: `tests/Entity/ArticleSlugTest.php`

- [ ] **Étape 1 : Écrire le test unitaire (doit échouer)**

Créer `tests/Entity/ArticleSlugTest.php` :

```php
<?php

namespace App\Tests\Entity;

use App\Entity\Article;
use PHPUnit\Framework\TestCase;

class ArticleSlugTest extends TestCase
{
    public function testSlugGenereDepuisTitre(): void
    {
        $article = new Article();
        $article->setTitre('Mon premier article');
        $article->generateSlug();

        $this->assertSame('mon-premier-article', $article->getSlug());
    }

    public function testSlugManuelNonEcrase(): void
    {
        $article = new Article();
        $article->setTitre('Mon premier article');
        $article->setSlug('slug-perso');
        $article->generateSlug();

        $this->assertSame('slug-perso', $article->getSlug());
    }

    public function testCaracteresSpeciauxNormalises(): void
    {
        $article = new Article();
        $article->setTitre('Ça & Là : "test"');
        $article->generateSlug();

        $this->assertSame('ca-la-test', $article->getSlug());
    }

    public function testTitreNullProduitsSlugNull(): void
    {
        $article = new Article();
        $article->generateSlug();

        $this->assertNull($article->getSlug());
    }
}
```

- [ ] **Étape 2 : Vérifier que le test échoue**

```bash
php bin/phpunit tests/Entity/ArticleSlugTest.php
```
Résultat attendu : FAIL — `Call to undefined method App\Entity\Article::generateSlug()`

- [ ] **Étape 3 : Ajouter le champ slug et la logique dans Article**

Modifier `src/Entity/Article.php` — ajouter l'import AsciiSlugger, `#[ORM\HasLifecycleCallbacks]`, le champ slug, les accesseurs et les méthodes lifecycle :

```php
// Ajouter cet import en haut du fichier, avec les autres use :
use Symfony\Component\String\Slugger\AsciiSlugger;

// Modifier la ligne #[ORM\Entity(...)] pour ajouter HasLifecycleCallbacks :
#[ORM\Entity(repositoryClass: ArticleRepository::class)]
#[ORM\HasLifecycleCallbacks]
class Article
{
    // ... champs existants ...

    /** Slug = URL conviviale ex: "mon-article" — doit être unique */
    #[ORM\Column(length: 255, unique: true, nullable: true)]
    private ?string $slug = null;

    // ... méthodes existantes ...

    public function getSlug(): ?string { return $this->slug; }

    public function setSlug(?string $slug): static { $this->slug = $slug; return $this; }

    #[ORM\PrePersist]
    public function onPrePersist(): void
    {
        $this->generateSlug();
    }

    #[ORM\PreUpdate]
    public function onPreUpdate(): void
    {
        $this->generateSlug();
    }

    /**
     * Génère le slug depuis le titre uniquement si aucun slug n'a été saisi manuellement.
     */
    public function generateSlug(): void
    {
        if (!empty($this->slug)) {
            return;
        }

        if (empty($this->titre)) {
            $this->slug = null;
            return;
        }

        $slugger = new AsciiSlugger('fr');
        $this->slug = strtolower($slugger->slug($this->titre));
    }
}
```

- [ ] **Étape 4 : Vérifier que les tests passent**

```bash
php bin/phpunit tests/Entity/ArticleSlugTest.php
```
Résultat attendu : OK (4 tests, 4 assertions)

- [ ] **Étape 5 : Générer et exécuter la migration**

```bash
php bin/console make:migration
php bin/console doctrine:migrations:migrate --no-interaction
```
Résultat attendu : migration créée dans `migrations/` et appliquée sans erreur.

- [ ] **Étape 6 : Lancer tous les tests**

```bash
php bin/phpunit
```
Résultat attendu : tous les tests passent (42 tests).

- [ ] **Étape 7 : Commit**

```bash
git add src/Entity/Article.php tests/Entity/ArticleSlugTest.php migrations/
git commit -m "[Blog] Ajout du slug auto-généré sur Article"
```

---

## Task 2 : ArticleRepository — méthodes de requête

**Files:**
- Modify: `src/Repository/ArticleRepository.php`

- [ ] **Étape 1 : Implémenter les méthodes dans ArticleRepository**

Remplacer le contenu de `src/Repository/ArticleRepository.php` par :

```php
<?php

namespace App\Repository;

use App\Entity\Article;
use Doctrine\Bundle\DoctrineBundle\Repository\ServiceEntityRepository;
use Doctrine\Persistence\ManagerRegistry;

class ArticleRepository extends ServiceEntityRepository
{
    public function __construct(ManagerRegistry $registry)
    {
        parent::__construct($registry, Article::class);
    }

    /**
     * Retourne tous les articles publiés, du plus récent au plus ancien.
     *
     * @return Article[]
     */
    public function findPublished(): array
    {
        return $this->createQueryBuilder('a')
            ->andWhere('a.statut = :statut')
            ->setParameter('statut', 'publie')
            ->orderBy('a.datePublication', 'DESC')
            ->getQuery()
            ->getResult();
    }

    /**
     * Retourne un article publié par son slug, ou null s'il n'existe pas.
     */
    public function findOnePublishedBySlug(string $slug): ?Article
    {
        return $this->createQueryBuilder('a')
            ->andWhere('a.statut = :statut')
            ->andWhere('a.slug = :slug')
            ->setParameter('statut', 'publie')
            ->setParameter('slug', $slug)
            ->getQuery()
            ->getOneOrNullResult();
    }
}
```

- [ ] **Étape 2 : Vérifier que tous les tests passent encore**

```bash
php bin/phpunit
```
Résultat attendu : OK (42 tests).

- [ ] **Étape 3 : Commit**

```bash
git add src/Repository/ArticleRepository.php
git commit -m "[Blog] ArticleRepository — findPublished et findOnePublishedBySlug"
```

---

## Task 3 : Service CommentaireHandler

**Files:**
- Create: `src/Service/CommentaireHandler.php`
- Create: `tests/Service/CommentaireHandlerTest.php`

- [ ] **Étape 1 : Écrire le test unitaire (doit échouer)**

Créer `tests/Service/CommentaireHandlerTest.php` :

```php
<?php

namespace App\Tests\Service;

use App\Entity\Article;
use App\Entity\Commentaire;
use App\Entity\User;
use App\Service\CommentaireHandler;
use Doctrine\ORM\EntityManagerInterface;
use PHPUnit\Framework\TestCase;

class CommentaireHandlerTest extends TestCase
{
    public function testSubmitAssocieAuteurArticleEtStatut(): void
    {
        $em = $this->createMock(EntityManagerInterface::class);
        $em->expects($this->once())->method('persist');
        $em->expects($this->once())->method('flush');

        $handler = new CommentaireHandler($em);

        $article = new Article();
        $auteur = new User();
        $commentaire = new Commentaire();
        $commentaire->setContenu('Super article !');

        $handler->submit($commentaire, $article, $auteur);

        $this->assertSame($article, $commentaire->getArticle());
        $this->assertSame($auteur, $commentaire->getAuteur());
        $this->assertSame('en_attente', $commentaire->getStatut());
    }
}
```

- [ ] **Étape 2 : Vérifier que le test échoue**

```bash
php bin/phpunit tests/Service/CommentaireHandlerTest.php
```
Résultat attendu : FAIL — `Class "App\Service\CommentaireHandler" not found`

- [ ] **Étape 3 : Créer le service CommentaireHandler**

Créer `src/Service/CommentaireHandler.php` :

```php
<?php

namespace App\Service;

use App\Entity\Article;
use App\Entity\Commentaire;
use App\Entity\User;
use Doctrine\ORM\EntityManagerInterface;

class CommentaireHandler
{
    public function __construct(private readonly EntityManagerInterface $em) {}

    /**
     * Associe l'auteur et l'article au commentaire, force le statut
     * en_attente (modération EasyAdmin) puis persiste en base.
     */
    public function submit(Commentaire $commentaire, Article $article, User $auteur): void
    {
        $commentaire->setArticle($article);
        $commentaire->setAuteur($auteur);
        $commentaire->setStatut('en_attente');

        $this->em->persist($commentaire);
        $this->em->flush();
    }
}
```

- [ ] **Étape 4 : Vérifier que le test passe**

```bash
php bin/phpunit tests/Service/CommentaireHandlerTest.php
```
Résultat attendu : OK (1 test, 3 assertions)

- [ ] **Étape 5 : Lancer tous les tests**

```bash
php bin/phpunit
```
Résultat attendu : OK (43 tests).

- [ ] **Étape 6 : Commit**

```bash
git add src/Service/CommentaireHandler.php tests/Service/CommentaireHandlerTest.php
git commit -m "[Blog] Service CommentaireHandler + test unitaire"
```

---

## Task 4 : Formulaire CommentaireType

**Files:**
- Create: `src/Form/CommentaireType.php`

- [ ] **Étape 1 : Créer le formulaire**

Créer `src/Form/CommentaireType.php` :

```php
<?php

namespace App\Form;

use App\Entity\Commentaire;
use Symfony\Component\Form\AbstractType;
use Symfony\Component\Form\Extension\Core\Type\TextareaType;
use Symfony\Component\Form\FormBuilderInterface;
use Symfony\Component\OptionsResolver\OptionsResolver;
use Symfony\Component\Validator\Constraints\Length;
use Symfony\Component\Validator\Constraints\NotBlank;

class CommentaireType extends AbstractType
{
    public function buildForm(FormBuilderInterface $builder, array $options): void
    {
        $builder->add('contenu', TextareaType::class, [
            'label' => 'Votre commentaire',
            'attr'  => ['rows' => 4, 'class' => 'form-control'],
            'constraints' => [
                new NotBlank(message: 'Le commentaire ne peut pas être vide.'),
                new Length(
                    min: 5,
                    minMessage: 'Le commentaire doit contenir au moins {{ limit }} caractères.'
                ),
            ],
        ]);
    }

    public function configureOptions(OptionsResolver $resolver): void
    {
        $resolver->setDefaults(['data_class' => Commentaire::class]);
    }
}
```

- [ ] **Étape 2 : Vérifier que tous les tests passent encore**

```bash
php bin/phpunit
```
Résultat attendu : OK (43 tests).

- [ ] **Étape 3 : Commit**

```bash
git add src/Form/CommentaireType.php
git commit -m "[Blog] Formulaire CommentaireType"
```

---

## Task 5 : ArticleController, templates et navbar

**Files:**
- Create: `src/Controller/ArticleController.php`
- Create: `templates/blog/index.html.twig`
- Create: `templates/blog/show.html.twig`
- Modify: `templates/base.html.twig`

- [ ] **Étape 1 : Écrire les tests fonctionnels (doivent échouer)**

Créer `tests/Controller/ArticleControllerTest.php` :

```php
<?php

namespace App\Tests\Controller;

use Symfony\Bundle\FrameworkBundle\Test\WebTestCase;

class ArticleControllerTest extends WebTestCase
{
    public function testIndexRetourne200(): void
    {
        $client = static::createClient();
        $client->request('GET', '/blog');

        $this->assertResponseIsSuccessful();
    }

    public function testSlugInexistantRetourne404(): void
    {
        $client = static::createClient();
        $client->request('GET', '/blog/article-qui-nexiste-pas');

        $this->assertResponseStatusCodeSame(404);
    }

    public function testCommentaireSansAuthRedirigeVersLogin(): void
    {
        $client = static::createClient();
        $client->request('POST', '/blog/un-slug/commenter');

        // Sans authentification, #[IsGranted] doit rediriger vers /login
        $this->assertResponseRedirects('/login');
    }
}
```

- [ ] **Étape 2 : Vérifier que les tests échouent**

```bash
php bin/phpunit tests/Controller/ArticleControllerTest.php
```
Résultat attendu : FAIL — `No route found for "GET /blog"`

- [ ] **Étape 3 : Créer ArticleController**

Créer `src/Controller/ArticleController.php` :

```php
<?php

namespace App\Controller;

use App\Entity\Commentaire;
use App\Form\CommentaireType;
use App\Repository\ArticleRepository;
use App\Service\CommentaireHandler;
use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\Routing\Attribute\Route;
use Symfony\Component\Security\Http\Attribute\IsGranted;

class ArticleController extends AbstractController
{
    /**
     * Liste tous les articles publiés.
     */
    #[Route('/blog', name: 'app_blog_index')]
    public function index(ArticleRepository $articleRepository): Response
    {
        return $this->render('blog/index.html.twig', [
            'articles' => $articleRepository->findPublished(),
        ]);
    }

    /**
     * Affiche un article publié avec ses commentaires approuvés et le formulaire de commentaire.
     */
    #[Route('/blog/{slug}', name: 'app_blog_show')]
    public function show(string $slug, ArticleRepository $articleRepository): Response
    {
        $article = $articleRepository->findOnePublishedBySlug($slug);

        if (!$article) {
            throw $this->createNotFoundException("Article introuvable : $slug");
        }

        $commentaires = $article->getCommentaires()->filter(
            fn(Commentaire $c) => $c->getStatut() === 'approuve'
        );

        $form = $this->createForm(CommentaireType::class, new Commentaire());

        return $this->render('blog/show.html.twig', [
            'article'      => $article,
            'commentaires' => $commentaires,
            'form'         => $form,
        ]);
    }

    /**
     * Reçoit la soumission du formulaire de commentaire.
     * Réservé aux utilisateurs connectés (#[IsGranted]).
     */
    #[Route('/blog/{slug}/commenter', name: 'app_blog_comment', methods: ['POST'])]
    #[IsGranted('IS_AUTHENTICATED_FULLY')]
    public function comment(
        string $slug,
        Request $request,
        ArticleRepository $articleRepository,
        CommentaireHandler $handler,
    ): Response {
        $article = $articleRepository->findOnePublishedBySlug($slug);

        if (!$article) {
            throw $this->createNotFoundException("Article introuvable : $slug");
        }

        $commentaire = new Commentaire();
        $form = $this->createForm(CommentaireType::class, $commentaire);
        $form->handleRequest($request);

        if ($form->isSubmitted() && $form->isValid()) {
            /** @var \App\Entity\User $user */
            $user = $this->getUser();
            $handler->submit($commentaire, $article, $user);
            $this->addFlash('success', 'Votre commentaire a été soumis et attend modération.');
        }

        return $this->redirectToRoute('app_blog_show', ['slug' => $slug]);
    }
}
```

- [ ] **Étape 4 : Créer le template blog/index.html.twig**

Créer `templates/blog/index.html.twig` :

```twig
{% extends 'base.html.twig' %}

{% block title %}Blog — CMS DISII{% endblock %}

{% block body %}
    <h1 class="mb-4">Blog</h1>

    {% if articles is empty %}
        <p class="text-muted">Aucun article publié pour le moment.</p>
    {% else %}
        <div class="row row-cols-1 row-cols-md-2 g-4">
            {% for article in articles %}
                <div class="col">
                    <div class="card h-100">
                        <div class="card-body">
                            <h2 class="card-title h5">
                                <a href="{{ path('app_blog_show', {slug: article.slug}) }}"
                                   class="text-decoration-none">
                                    {{ article.titre }}
                                </a>
                            </h2>
                            <div class="mb-2">
                                {% if article.categorieArticle %}
                                    <span class="badge bg-primary">{{ article.categorieArticle.nom }}</span>
                                {% endif %}
                                {% for tag in article.tags %}
                                    <span class="badge bg-secondary">{{ tag.nom }}</span>
                                {% endfor %}
                            </div>
                            {% if article.contenu %}
                                <p class="card-text text-muted">
                                    {{ article.contenu|striptags|slice(0, 150) }}…
                                </p>
                            {% endif %}
                        </div>
                        <div class="card-footer text-muted small">
                            {% if article.datePublication %}
                                {{ article.datePublication|date('d/m/Y') }}
                            {% endif %}
                            {% if article.auteur %}
                                — {{ article.auteur.nom }}
                            {% endif %}
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
{% endblock %}
```

- [ ] **Étape 5 : Créer le template blog/show.html.twig**

Créer `templates/blog/show.html.twig` :

```twig
{% extends 'base.html.twig' %}

{% block title %}{{ article.titre }} — CMS DISII{% endblock %}

{% block body %}
    <article>
        <h1 class="mb-2">{{ article.titre }}</h1>

        <p class="text-muted small mb-4">
            {% if article.datePublication %}
                Publié le {{ article.datePublication|date('d/m/Y') }}
            {% endif %}
            {% if article.auteur %}
                par {{ article.auteur.nom }}
            {% endif %}
            {% if article.categorieArticle %}
                —
                <span class="badge bg-primary">{{ article.categorieArticle.nom }}</span>
            {% endif %}
            {% for tag in article.tags %}
                <span class="badge bg-secondary">{{ tag.nom }}</span>
            {% endfor %}
        </p>

        <div class="content mb-5">
            {{ article.contenu|raw }}
        </div>
    </article>

    <section class="mt-4">
        <h2 class="h4 mb-3">Commentaires ({{ commentaires|length }})</h2>

        {% for commentaire in commentaires %}
            <div class="card mb-3">
                <div class="card-body">
                    <p class="card-text">{{ commentaire.contenu }}</p>
                    <p class="card-text text-muted small">
                        {% if commentaire.auteur %}{{ commentaire.auteur.nom }} — {% endif %}
                        {{ commentaire.date|date('d/m/Y H:i') }}
                    </p>
                </div>
            </div>
        {% else %}
            <p class="text-muted">Aucun commentaire pour le moment.</p>
        {% endfor %}

        <hr class="my-4">

        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <h3 class="h5 mb-3">Laisser un commentaire</h3>

            {% for flash in app.flashes('success') %}
                <div class="alert alert-success">{{ flash }}</div>
            {% endfor %}

            {{ form_start(form, {action: path('app_blog_comment', {slug: article.slug}), method: 'POST'}) }}
                {{ form_row(form.contenu) }}
                <button type="submit" class="btn btn-primary mt-2">Envoyer</button>
            {{ form_end(form) }}
        {% else %}
            <p class="text-muted">
                <a href="{{ path('app_login') }}">Connectez-vous</a> pour laisser un commentaire.
            </p>
        {% endif %}
    </section>
{% endblock %}
```

- [ ] **Étape 6 : Ajouter le lien Blog dans la navbar**

Dans `templates/base.html.twig`, ajouter un `<li>` pour le lien Blog **après** la boucle `{% for page in menu_pages.menuPages %}` :

```twig
{# Remplacer le bloc <ul class="navbar-nav me-auto"> existant par : #}
<ul class="navbar-nav me-auto">
    {% for page in menu_pages.menuPages %}
        <li class="nav-item">
            <a class="nav-link" href="{{ path('app_page_show', {slug: page.slug}) }}">
                {{ page.titre }}
            </a>
        </li>
    {% endfor %}
    <li class="nav-item">
        <a class="nav-link" href="{{ path('app_blog_index') }}">Blog</a>
    </li>
</ul>
```

- [ ] **Étape 7 : Vérifier que les tests passent**

```bash
php bin/phpunit tests/Controller/ArticleControllerTest.php
```
Résultat attendu : OK (3 tests, 3 assertions)

- [ ] **Étape 8 : Lancer tous les tests**

```bash
php bin/phpunit
```
Résultat attendu : OK (46 tests).

- [ ] **Étape 9 : Commit**

```bash
git add src/Controller/ArticleController.php templates/blog/ templates/base.html.twig tests/Controller/ArticleControllerTest.php
git commit -m "[Blog] Contrôleur front + templates index et show + lien navbar"
```
